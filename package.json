{
  "name": "dahua-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx concurrently -k \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\" \"node local-agent.js\" \"node rtsp-proxy.js\" \"go2rtc.exe\"",
    "start": "npx concurrently -k \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\" \"node local-agent.js\" \"node rtsp-proxy.js\" \"go2rtc.exe\"",
    "agent": "node local-agent.js",
    "go2rtc": "go2rtc.exe",
    "dvr": "node local-dvr.js",
    "build": "npm --prefix frontend run build",
    "deploy": "npm run build && node auto-push.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "node-onvif": "^0.1.7",
    "ws": "^8.21.3"
  }
}
